<div class="user-banner">
<h1> Welcome <%= @user.name if @user.name %> !</h1>
</div>
<div class="row">
  <div class="large-12 columns user-intro">
    <h2 class="header-quote">"We travel not to escape life, but for life not to escape us" - Anonymous</h2>
    <p>Thank you for choosing Backpacker to plan your next big adventure! We understand the importance of pre-trip planning and you'll find some great tools on this site to make sure you're prepared. Click on the Start a New Trip button below to get started!</p>
  </div>
</div>
<div class="row">
<div class="large-4 columns user-trips">
	<h1>Your Trips</h1>
	<% if @trips.length > 0 %>
		<% @trips.each do |trip| %>
			<p><%= link_to trip.name, trip %></p>
		<% end %>
	<% else %>
		<h2>You don't have any existing trips.</h2>
	<% end %>
</div>
<div class="large-4 columns">
	<%= button_to "Start a new Trip", new_trip_path, :class => "new-trip-btn", :method => :get %>
</div>
<div class="large-4 columns">
</div>
    
