<nav class="top-bar" data-topbar role="navigation">
  <ul class="title-area">
    <% if user_signed_in? %>
      <li class="name"><h1><%= link_to 'BackPackers', user_path(current_user) %></h1></li>
    <% else %>
      <li><h1><%= link_to 'BackPackers', root_path %></h1></li>
    <% end %>
  </ul>

  <section class="top-bar-section">
    <!-- Right Nav Section -->
    <ul class="right">
      <%= render 'layouts/navigation_links' %>
      <%= render 'layouts/nav_links_for_auth' %>
    </ul>

  </section>
</nav>